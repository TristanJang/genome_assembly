for isolate in $(cat isolates.txt); do echo $isolate && grep -c ">" $isolate/assembly/circ/06.fixstart.fasta &&  awk '/^>/ {if (seqlen){print seqlen}; print ;seqlen=0;next; } { seqlen += length($0)}END{print seqlen}' $isolate/assembly/circ/06.fixstart.fasta;done;